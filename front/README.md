# Setup do Backend

Com o node instalado, basta rodar os comandos

    $ npm install
    $ npm run dev

Abra o seu navegador em [http://localhost:3000](http://localhost:3000) para verificar o servidor está rodando.
